import React from "react";
import { Grid, Typography } from "@material-ui/core";

function Login(){
  return (
    <Grid item>
      <Typography variant="h1">Faça login para acessar seus dados</Typography>
    </Grid>
  )
};

export default Login;